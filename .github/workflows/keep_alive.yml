name: TARA Sentinel
on:
  schedule:
    - cron: '0 */4 * * *' # Runs every 4 hours (Aggressive Keep-Alive)
  workflow_dispatch:

jobs:
  ping_scanner:
    runs-on: ubuntu-latest
    steps:
      - name: Ping TARA Space
        run: curl -I https://huggingface.co/spaces/YOUR_USERNAME/TARA-Institutional-Scanner
